// REMOVED: unused UI component. Left as stub to avoid accidental imports failures.

// The full Carousel implementation was removed because it had no references
// in the codebase. Keep a minimal export to avoid breaking imports.

export const __REMOVED__ = undefined;
